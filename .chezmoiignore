{{ if ne .chezmoi.os "windows" }}
AppData\Local\Packages\Microsoft.WindowsTerminal_8wekyb3d8bbwe\LocalState\settings.json
{{ end }}

# Ignore Windows-specific paths on non-Windows
{{ if ne .chezmoi.os "windows" }}
AppData/
{{ end }}

# Ignore macOS-specific paths on non-macOS
{{ if ne .chezmoi.os "darwin" }}
.hammerspoon/
Library/
{{ end }}

# Ignore Linux-only .config paths on non-Linux
# Note: starship.toml works on ALL platforms (Windows, macOS, Linux) - not ignored
# Note: zellij works on Unix only (macOS, Linux) - ignored on Windows below
{{ if ne .chezmoi.os "linux" }}
.config/btop/
.config/ghostty/
.config/lazygit/
.config/k9s/
{{ end }}

# Ignore Unix-only bin scripts on Windows
{{ if eq .chezmoi.os "windows" }}
.local/bin/clip
.local/bin/xdg-open
{{ end }}

# macOS: Use Library paths instead of .config paths for most apps
# (starship uses .config on all platforms; zellij uses .config on Unix)
{{ if eq .chezmoi.os "darwin" }}
# btop, ghostty, lazygit, k9s already ignored above via non-Linux condition
{{ end }}

# Windows: Ignore Unix-only tools
{{ if eq .chezmoi.os "windows" }}
AppData/Local/btop/
AppData/Local/zellij/
.config/zellij/
{{ end }}

# Ignore sh bootstrap script on Windows (target path)
{{ if eq .chezmoi.os "windows" }}
bootstrap.sh
{{ end }}

# Ignore ps1 bootstrap script on Unix (target path)
{{ if ne .chezmoi.os "windows" }}
bootstrap.ps1
{{ end }}
